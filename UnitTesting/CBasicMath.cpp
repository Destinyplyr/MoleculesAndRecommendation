#include "CBasicMath.hpp"

int CBasicMath::Addition(int x, int y)
{
   return (x + y);
}

int CBasicMath::Multiply(int x, int y)
{
   return (x * y);
}

int CBasicMath2::Addition(int x, int y)
{
   return (x + y);
}

int CBasicMath2::Multiply(int x, int y)
{
   return (x * y);
}

